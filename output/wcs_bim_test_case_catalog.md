# WCS-BIM Test Case Catalog

Generated by `scripts/generate-test-catalog.py`.

## Tier mapping
| Catalog suite | WCS-BIM target |
|---------------|----------------|
| Unit | WCS-BIMTests (fast: test-fast.sh) |
| UI | WCS-BIMUITests Tier1–3 |
| Integration | ModelContainerBootstrapTests, AppSettingsUpdaterTests |
| Perf | Tier5PerformanceUITests |
| Manual | Not automated |

## Auth

- **Valid login** (n/a) — Input: Known user credentials; Expected: Session created, home opens; Suite: UI/Integration; Test: `—`.
- **Invalid login** (n/a) — Input: Bad password; Expected: Error shown, no session; Suite: Unit/UI; Test: `—`.
- **Session restore** (n/a) — Input: Existing token; Expected: User returns to last state; Suite: Integration; Test: `—`.

## Onboarding

- **First launch** (partial) — Input: Fresh install; Expected: Intro flow appears once; Suite: UI; Test: `WCS_BIMUITestsLaunchTests.testLaunch`.
- **Skip onboarding** (implemented) — Input: Skip action; Expected: Home loads immediately; Suite: UI; Test: `Tier1SmokeUITests.testAppShellTabsVisible`.

## Core

- **Primary action success** (implemented) — Input: Valid payload; Expected: Action completes and persists; Suite: Integration/UI; Test: `Tier1SmokeUITests.testCreateProjectCoreAction`.
- **Primary action failure** (partial) — Input: Server error; Expected: Graceful error and retry path; Suite: Unit/Integration; Test: `OpenAIResponseContractTests`.

## Sync

- **Offline save** (implemented) — Input: No network; Expected: Local queue stores data; Suite: Unit/Integration; Test: `ModelContainerBootstrapTests`.
- **Reconnect sync** (partial) — Input: Network restored; Expected: Queued data syncs; Suite: Integration; Test: `SwiftData disk store (ArchFusion_v11)`.

## Settings

- **Change preference** (implemented) — Input: Toggle setting; Expected: Setting persists after relaunch; Suite: Unit/Integration/UI; Test: `AppSettingsUpdaterTests + Tier3ScreenUITests.testSettingsScreen`.

## Notifications

- **Permission grant** (planned) — Input: Allow notifications; Expected: Token registered; Suite: Manual/UI; Test: `—`.
- **Permission deny** (planned) — Input: Deny notifications; Expected: Fallback path works; Suite: Manual/UI; Test: `—`.

## Performance

- **Cold launch** (implemented) — Input: App starts from terminated state; Expected: Launch within target; Suite: Perf; Test: `Tier5PerformanceUITests.testLaunchPerformance`.
- **Scroll smoothness** (implemented) — Input: Long feed; Expected: No visible jank; Suite: Perf/UI; Test: `AllUIUnitsUITests.testAllUIUnitsEndToEnd`.
